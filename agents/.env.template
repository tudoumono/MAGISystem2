# MAGI Agents Configuration Template
# このファイルをコピーして .env として使用してください

# AWS基本設定
# Amplify Hostingでは「AWS_」で始まる環境変数は設定不可のため「APP_」を使用
APP_AWS_REGION=ap-northeast-1

# 後方互換性のため（ローカル開発のみ）
# Amplify Hostingでは以下は設定しないでください
# AWS_REGION=ap-northeast-1
# AWS_DEFAULT_REGION=ap-northeast-1

# MAGI AgentCore Runtime
# .bedrock_agentcore.yamlから取得した値を設定してください
MAGI_AGENT_ARN=arn:aws:bedrock-agentcore:REGION:ACCOUNT_ID:runtime/AGENT_ID
MAGI_AGENT_ID=your_agent_id

# ------------------------------------------------------------------------------
# AgentCore CLI による環境変数設定とデプロイ
# ------------------------------------------------------------------------------
# 注意: "agentcore.exe" というWindowsバイナリは存在しません
# Amazon Bedrock AgentCoreは、Pythonベースの "agentcore" CLIコマンドを提供します
#
# 環境変数を設定してデプロイする方法:
#   agentcore launch \
#     --env AWS_REGION=ap-northeast-1 \
#     --env AWS_DEFAULT_REGION=ap-northeast-1 \
#     --env MAGI_AGENT_ARN=arn:aws:bedrock-agentcore:... \
#     --env MAGI_AGENT_ID=your_agent_id \
#     --env DEBUG_STREAMING=true \
#     --env VERBOSE_OUTPUT=true \
#     --env TEST_OUTPUT_DIR=tests/streaming_output \
#     --auto-update-on-conflict
#
# 主要なフラグ:
#   --env KEY=VALUE           環境変数を設定（複数指定可能）
#   --auto-update-on-conflict 既存エージェントを自動更新
#   --agent AGENT_NAME        エージェント名を明示的に指定
#
# 重要: AWSマネジメントコンソールUIでは環境変数を直接設定できません
#       CLI、CloudFormation、またはAWS CLIを使用する必要があります
#
# 参考リンク:
#   - AgentCore CLI: https://aws.github.io/bedrock-agentcore-starter-toolkit/api-reference/cli.html
#   - Runtime Quickstart: https://aws.github.io/bedrock-agentcore-starter-toolkit/user-guide/runtime/quickstart.html
#   - 環境変数設定方法: https://zenn.dev/kobank_t/articles/20250818-bedrock-agentcore-env
#   - AWS CLI Reference: https://docs.aws.amazon.com/cli/latest/reference/bedrock-agentcore-control/create-agent-runtime.html
# ------------------------------------------------------------------------------

# AWS認証情報（ローカル開発用）
# 本番環境ではIAM Roleを使用するため不要
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# デバッグ設定
DEBUG_STREAMING=true
VERBOSE_OUTPUT=true

# テスト設定
TEST_OUTPUT_DIR=tests/streaming_output

# パフォーマンス設定
REQUEST_TIMEOUT=300
CONNECT_TIMEOUT=10
MAX_RETRIES=3

# カスタムプロンプト設定（オプション）
# 各賢者のロール説明をカスタマイズできます
# 注意: JSON出力形式は自動的に追加されるため、ロール説明のみを記述してください

# CASPAR（保守的・現実的な賢者）のカスタムプロンプト
# CASPAR_CUSTOM_PROMPT="あなたはCASPARです。保守的で現実的な視点から判断してください。"

# BALTHASAR（革新的・感情的な賢者）のカスタムプロンプト
# BALTHASAR_CUSTOM_PROMPT="あなたはBALTHASARです。革新的で創造的な視点から判断してください。"

# MELCHIOR（バランス型・科学的な賢者）のカスタムプロンプト
# MELCHIOR_CUSTOM_PROMPT="あなたはMELCHIORです。データと論理に基づいてバランスよく判断してください。"

# SOLOMON（統括AI）のカスタムプロンプト
# 注意: {sage_responses} プレースホルダーは省略可能です（自動的に追加されます）
# SOLOMON_CUSTOM_PROMPT="あなたはSOLOMONです。3賢者の判断を統合評価してください。"

# 回答の文字数制限（カスタマイズ可能）
# 3賢者の reasoning フィールドの最大文字数（デフォルト: 1000）
SAGE_REASONING_MAX_LENGTH=1000

# SOLOMONの reasoning フィールドの最大文字数（デフォルト: 1500）
SOLOMON_REASONING_MAX_LENGTH=1500
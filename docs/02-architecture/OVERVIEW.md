# MAGI Decision System - アーキテクチャ設計書

## 📚 参考記事

このプロジェクトは以下の記事のアーキテクチャを**完全準拠**で実装します：

**[Amplify HostingでBedrock AgentCoreを使う](https://qiita.com/moritalous/items/ea695f8a328585e1313b)**

## 🏗️ システム構成

### 全体アーキテクチャ

⚠️ **重要な注意事項**: 
- AgentCore Runtimeは**1つのDockerコンテナ**です
- Next.jsバックエンドとPythonエージェントは**同じコンテナ内**で動作します
- Next.jsがHTTPリクエストを受け、Pythonを**子プロセス**として呼び出します

```
┌─────────────────────────────────────────────────────────────┐
│                 Amplify Hosting                              │
│  Next.js Frontend (既存)                                     │
│  ├─ Chat Interface (useChat)                                │
│  ├─ Agent Response Panels                                   │
│  ├─ SOLOMON Judge Display                                   │
│  ├─ Trace Visualization                                     │
│  ├─ Auth (Cognito)                                          │
│  └─ Data (DynamoDB + GraphQL)                              │
└─────────────────────────────────────────────────────────────┘
                         │ useChat() → /invocations
                         ▼
┌─────────────────────────────────────────────────────────────┐
│     Bedrock AgentCore Runtime (1つのDockerコンテナ)          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Next.js Backend (新規作成)                           │  │
│  │  - ポート8080でHTTPリクエスト受信                      │  │
│  │  - POST /invocations                                  │  │
│  │  - GET /ping                                          │  │
│  │  ↓ spawn('python', ['magi_agent.py'])                │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Python Agents (既存)                                 │  │
│  │  - 同一コンテナ内で子プロセスとして実行                │  │
│  │  - magi_agent.py                                      │  │
│  │  - Strands Agents 1.0                                 │  │
│  │  - CASPAR / BALTHASAR / MELCHIOR / SOLOMON           │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                              │
│  Dockerfile: python:3.11-slim + Node.js                     │
│  CMD: npm start (Next.jsバックエンドを起動)                  │
└─────────────────────────────────────────────────────────────┘
                         │ InvokeModel
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              Amazon Bedrock API                              │
│  - Claude 3.5 Sonnet                                         │
│  - ストリーミングレスポンス                                   │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 設計判断

### なぜこのアーキテクチャを選択したか

1. **Amplify Hostingのストリーミング制限を回避**
   - Amplify HostingはServer-Sent Eventsのストリーミングに対応していない
   - AgentCore Runtime内でNext.jsバックエンドを動かすことで制限を回避

2. **実証済みのパターン**
   - 参考記事で動作確認済み
   - 安定性と信頼性が保証されている

3. **既存資産の活用**
   - Python実装（Strands Agents）をそのまま活用
   - フロントエンド（Next.js）もそのまま活用
   - 無駄な再実装を避ける

4. **シンプルな構成**
   - Lambda Response Streamingなどの複雑な設定不要
   - 保守しやすい

## 📁 ディレクトリ構造

### プロジェクト全体

```
MAGISystem2/
├── src/                          # フロントエンド（Amplify Hosting）
│   ├── app/
│   │   ├── dashboard/           # メインUI
│   │   └── api/                 # 使用しない（削除予定）
│   ├── components/
│   └── hooks/
│       └── useMAGIChat.ts       # useChat()統合
├── agents/                       # AgentCore Runtime（1つのコンテナ）
│   ├── backend/                 # 新規作成: Next.jsバックエンド
│   │   ├── package.json
│   │   ├── next.config.js
│   │   ├── app/
│   │   │   └── api/
│   │   │       ├── invocations/route.ts
│   │   │       └── ping/route.ts
│   │   └── lib/
│   │       └── python-bridge.ts
│   ├── magi_agent.py            # 既存: Pythonエージェント
│   ├── shared/
│   ├── requirements.txt
│   └── Dockerfile               # Next.js + Python統合
└── amplify/                      # Amplify設定
    ├── auth/
    └── data/
```

### AgentCore Runtimeコンテナ内部（agents/ディレクトリ）

⚠️ **重要**: これらは全て**1つのDockerコンテナ内**に含まれます

```
agents/ (Dockerコンテナ内)
├── backend/                     # Next.jsバックエンド
│   ├── .next/                  # ビルド済みNext.js
│   ├── node_modules/
│   └── package.json
├── magi_agent.py                # Pythonエージェント
├── shared/
│   ├── types.py
│   └── prompts.py
├── venv/                        # Python仮想環境
└── requirements.txt

実行時:
- Next.jsがポート8080でHTTPリクエストを受信
- Next.jsがPythonを子プロセスとして起動
- 両方が同じコンテナ内で動作
```

## 🔧 技術スタック

### フロントエンド（Amplify Hosting）
- **Next.js 15** + **React 19** + **TypeScript**
- **Tailwind CSS**
- **Vercel AI SDK** (`useChat`)
- **Amplify Auth** (Cognito)
- **Amplify Data** (DynamoDB + GraphQL)

### バックエンド（AgentCore Runtime）
- **Next.js** (TypeScript) - API Routes
- **Python 3.11+** - Strands Agents
- **Strands Agents 1.0** - エージェントフレームワーク
- **Amazon Bedrock** - LLMモデル

## 🚀 通信フロー

### 1. ユーザーが質問を入力

```typescript
// src/hooks/useMAGIChat.ts
import { useChat } from 'ai/react';

export function useMAGIChat() {
  return useChat({
    api: process.env.NEXT_PUBLIC_AGENTCORE_URL + '/invocations'
  });
}
```

### 2. AgentCore Runtime（Next.jsバックエンド）が受信

⚠️ **重要**: Next.jsとPythonは**同じコンテナ内**で動作します

```typescript
// agents/backend/app/api/invocations/route.ts
import { spawn } from 'child_process';

export async function POST(request: Request) {
  const { messages } = await request.json();
  
  // 同じコンテナ内のPythonスクリプトを子プロセスとして呼び出し
  const pythonProcess = spawn('python', [
    '/app/magi_agent.py',  // コンテナ内の絶対パス
    '--question', messages[messages.length - 1].content
  ]);
  
  // Pythonの標準出力をストリーミングレスポンスとして転送
  return new Response(pythonProcess.stdout, {
    headers: { 'Content-Type': 'text/event-stream' }
  });
}
```

### 3. Python magi_agent.pyが処理

```python
# agents/magi_agent.py
async def process_decision(question: str):
    # 3賢者並列実行
    results = await asyncio.gather(
        caspar_agent.analyze(question),
        balthasar_agent.analyze(question),
        melchior_agent.analyze(question)
    )
    
    # SOLOMON Judge統合
    final_decision = await solomon_judge.evaluate(results)
    
    return final_decision
```

## ⚠️ 重要な注意事項

### 実装状況（2025-11-09更新）

✅ **参考記事のコンセプト + Python統合（実装完了）**
- **参考記事から採用**:
  - AgentCore Runtime（Dockerコンテナ）のコンセプト
  - /invocations エンドポイント
  - ポート8080での起動
  - フロントエンドからの直接呼び出し

- **MAGI独自の拡張**:
  - `agents/backend/` - Next.jsバックエンドで spawn() を使用
  - `agents/Dockerfile` - Next.js + Python統合コンテナ
  - `POST /api/invocations` - spawn()でPythonプロセス起動
  - `magi_agent.py` - AWS Strands Agentsを使用した3賢者システム

⚠️ **参考記事との違い**
- 参考記事: Next.jsから直接 `@ai-sdk/amazon-bedrock` でBedrock呼び出し
- MAGI実装: Next.jsから spawn() でPythonエージェント起動 → Strands Agents使用

⚠️ **PR #5で誤って実装されたファイル（非推奨）**
- `src/app/api/magi/stream/route.ts` - BedrockAgentCoreClient使用（誤った方向性）
- これらは学習目的のため残されていますが、使用しないでください

### 使用しないパターン

以下のパターンは**使用しません**：

❌ **AWS SDK経由のAgentCore Runtime呼び出し**
- PR #5で誤って実装されたパターン
- 参考記事のアーキテクチャとは異なる
- `BedrockAgentCoreClient` を使用しない

❌ **Lambda Response Streaming**
- 複雑で未実証
- Amplify Gen2での設定が困難

❌ **Amplify defineFunction()**
- ストリーミングに対応していない
- 参考記事のパターンと異なる

### 採用するパターン

✅ **参考記事のコンセプト + Python統合（実装完了）**
- **参考記事のコンセプト**: AgentCore Runtime（Dockerコンテナ、/invocations）
- **独自拡張**: Next.jsからspawn()でPythonプロセス起動
- **既存資産活用**: AWS Strands Agentsを使用した3賢者システム（magi_agent.py）
- **メリット**:
  - Amplify Hostingのストリーミング制限を回避
  - 既存のPythonコードを再利用
  - Strands Agentsの強力な機能を活用
  - シンプルで保守しやすい

## 📚 参考資料

- [参考記事: Amplify HostingでBedrock AgentCoreを使う](https://qiita.com/moritalous/items/ea695f8a328585e1313b)
- [Strands Agents公式ドキュメント](https://strandsagents.com/latest/)
- [Vercel AI SDK](https://sdk.vercel.ai/docs)
- [Amazon Bedrock](https://aws.amazon.com/bedrock/)
